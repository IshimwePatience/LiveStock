# Backend server URL (no trailing slash).
# Local dev:  http://localhost:5000
# Production: leave empty when nginx proxies /api on the same domain
VITE_SERVER_URL=http://localhost:5000

# Optional — only set if socket runs on a different host than the API
# VITE_SOCKET_URL=
